{
  "name": "functions",
  "description": "Cloud Functions for scheduled daily cleanup",
  "scripts": {
    "build": "echo 'no build'",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0"
  },
  "private": true
}
